// src/rules/custom/index.ts
// Project-specific custom rules
// Add your own rules here for project-specific abstractions

import type { Rule } from '../../types/rule';

// Example: ConfigReader.getProperty("url") → process.env.URL
export const customRules: Rule[] = [
  // Add project-specific rules here
];
